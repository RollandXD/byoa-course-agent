[project]
name = "byoa-course-agent"
version = "0.2.0"
description = "BYOA Code: a Claude Code style DeepSeek terminal agent for the BYOA experiment."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.byoa-agent]
workspace = ".."

